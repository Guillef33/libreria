import React from "react";
import ReactDom from "react-dom";
import books from "./books";
import { Button } from "./Button";



//CSS
import "./index.css";

// Constants

// function CategoryBookList {

//   return (

//    category==='all'? booksPerCategory.map(({ books, category }, index) => 
//      (
//       <div key={index}>
//         <h4>{category.value}</h4>
//         {posts.map((post, index) => {
//           return (
//             <article key={index}>
//               {post.title.rendered}
//             </article>
//           )
//         })}
//       </div>
//      ) : postsPerCategory.map(({ posts, category }, index) => {
//     return categoryName === category.name? (
//       <div key={index}>
//         <h4>{category.name}</h4>
//         {posts.map((post, index) => {
//           return (
//             <article key={index}>
//               {post.title.rendered}
//             </article>
//           )
//         })}
//       </div>
//     ) : null
//   })
//   )
// }
// }

export default CategoryBookList;
